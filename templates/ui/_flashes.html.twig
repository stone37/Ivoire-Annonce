{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="notify" app-data="{{ label }}" style="display: none;">{{ message }}</div>
    {% endfor %}
{% endfor %}

